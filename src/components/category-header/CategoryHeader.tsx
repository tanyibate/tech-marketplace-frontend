import React from "react";
import styles from "./category-header-styles.module.scss";

export default function CategoryHeader({ category }) {
  return (
    <div className="w-full bg-black">
      <p>Category</p>
    </div>
  );
}
